import {createContext} from 'react'

export const AppContext = createContext({
  expoToken: null,
  // loginData: null,
  // login: async () => {
  // },
  // logout: async () => {
  // },
  setToken: async () => {
  }
})
